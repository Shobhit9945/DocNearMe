// All clinic data lives in MongoDB — no local seed data.
import type { ClinicDoctor, ClinicProfile } from "./api";

export const CLINIC_SEED: ClinicProfile[] = [];
export const DOCTOR_SEED: ClinicDoctor[] = [];
